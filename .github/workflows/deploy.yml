name: 🚀 自动部署到腾讯云

on:
  push:
    branches:
      - master
      - main

jobs:
  deploy:
    name: 部署到服务器
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
      
      - name: 🔐 配置 SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
      
      - name: 🚀 部署到服务器
        run: |
          ssh -i ~/.ssh/deploy_key root@${{ secrets.SERVER_HOST }} bash << 'ENDSSH'
            echo "========================================="
            echo "  开始自动部署 - 铃铛师兄大模型博客"
            echo "========================================="
            
            cd /root/app/LLMBlog
            
            echo ""
            echo "📥 拉取最新代码..."
            git pull origin master
            if [ $? -ne 0 ]; then
              echo "❌ 拉取代码失败"
              exit 1
            fi
            
            echo ""
            echo "💾 备份数据库..."
            mkdir -p backups
            DATE=$(date +%Y%m%d_%H%M%S)
            docker compose exec -T mysql mysqldump -uroot -proot123456 lingdang_blog 2>/dev/null > backups/backup_$DATE.sql || echo "备份失败，继续部署"
            
            echo ""
            echo "🔄 停止旧服务..."
            docker compose down
            
            echo ""
            echo "🏗️ 重新构建镜像..."
            docker compose build --no-cache
            
            echo ""
            echo "🚀 启动所有服务..."
            docker compose up -d
            
            echo ""
            echo "⏳ 等待服务启动..."
            sleep 30
            
            echo ""
            echo "📊 检查服务状态..."
            docker compose ps
            
            echo ""
            echo "🔍 验证后端API..."
            if curl -f -s http://localhost:8080/api/posts > /dev/null 2>&1; then
              echo "✅ 后端正常"
            else
              echo "⚠️ 后端可能未就绪"
            fi
            
            echo ""
            echo "🔍 验证前端..."
            if curl -f -s http://localhost > /dev/null 2>&1; then
              echo "✅ 前端正常"
            else
              echo "⚠️ 前端可能未就绪"
            fi
            
            echo ""
            echo "========================================="
            echo "✅ 部署完成！"
            echo "========================================="
          ENDSSH
      
      - name: ✅ 部署成功通知
        if: success()
        run: |
          echo "🎉 部署成功！"
          echo "访问地址: http://${{ secrets.SERVER_HOST }}"
      
      - name: ❌ 部署失败通知
        if: failure()
        run: |
          echo "❌ 部署失败，请检查日志"

# 🚀 部署脚本说明

## 📂 文件说明

### 核心部署脚本

| 文件 | 用途 | 使用时机 |
|------|------|----------|
| **`../deploy.sh`** | 首次部署 | 全新服务器第一次部署 |
| **`deploy.sh`** | 更新部署 | 更新代码或重新部署 |

### 高级配置文件

| 文件 | 用途 | 适用场景 |
|------|------|----------|
| `nginx-ssl.conf` | HTTPS 配置 | 配置 SSL 证书时使用 |
| `Jenkinsfile` | Jenkins CI/CD | 企业级自动化部署 |

---

## 🎯 使用指南

### 场景 1: 首次部署（全新服务器）

```bash
# 在服务器上
cd /root/demo
chmod +x deploy.sh
sudo bash deploy.sh
```

**功能：**
- ✅ 安装 Docker
- ✅ 安装 Docker Compose
- ✅ 配置防火墙
- ✅ 启动所有服务

---

### 场景 2: 更新部署（已部署过的服务器）

```bash
# 在服务器上
cd /root/demo
bash deploy/deploy.sh
```

**功能：**
- ✅ 自动备份数据库
- ✅ Git pull 拉取最新代码
- ✅ 重新构建镜像
- ✅ 健康检查

---

### 场景 3: 配置 HTTPS

```bash
# 1. 获取 SSL 证书
certbot certonly --standalone -d your-domain.com

# 2. 使用 HTTPS 配置
cp deploy/nginx-ssl.conf frontend/nginx.conf

# 3. 重新部署
bash deploy/deploy.sh
```

---

### 场景 4: Jenkins 自动化（高级）

如果你要使用 Jenkins CI/CD：

1. 安装 Jenkins
2. 创建 Pipeline 项目
3. 使用 `deploy/Jenkinsfile`
4. 配置 Git 仓库

**注意：** 个人博客不推荐使用 Jenkins，太复杂了。

---

## 📋 快速参考

### 首次部署
```bash
# 方法1: 一键部署（推荐）
bash deploy.sh

# 方法2: Docker Compose
docker-compose up -d --build
```

### 日常更新
```bash
# 方法1: 更新脚本（推荐，带备份）
bash deploy/deploy.sh

# 方法2: 手动更新
git pull
docker-compose down
docker-compose up -d --build
```

### 查看状态
```bash
docker-compose ps
docker-compose logs -f
```

### 停止服务
```bash
docker-compose down
```

---

## 💡 最佳实践

### 推荐工作流

1. **首次部署：**
   ```bash
   sudo bash deploy.sh  # 根目录的，完整初始化
   ```

2. **日常更新：**
   ```bash
   bash deploy/deploy.sh  # deploy文件夹的，带备份
   ```

3. **紧急修复：**
   ```bash
   # 直接修改代码后
   docker-compose up -d --build
   ```

---

## 🔍 区别总结

| 特性 | 根目录 deploy.sh | deploy/deploy.sh |
|------|-----------------|------------------|
| 安装 Docker | ✅ | ❌ |
| 配置防火墙 | ✅ | ❌ |
| 系统更新 | ✅ | ❌ |
| **数据备份** | ❌ | ✅ 🔥 |
| **Git pull** | ❌ | ✅ |
| **健康检查** | ❌ | ✅ |
| 使用场景 | 首次部署 | 更新部署 |

---

## 📞 需要帮助？

- 首次部署问题：查看 `../DEPLOYMENT_GUIDE.md`
- 日常使用问题：查看 `../README.md`
- HTTPS 配置：查看 `nginx-ssl.conf`
